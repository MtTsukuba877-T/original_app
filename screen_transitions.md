# 卒業制作MVP 画面遷移設計書

**作成日**: 2026年6月28日
**バージョン**: MVP版設計確定

---

## 1. プロジェクト概要

### 1.1 サービスコンセプト

産業保健スタッフ向けのストレスチェックアプリ。**BtoBtoEモデル**で、契約企業(B)が支払い、産業保健スタッフ(B)が運用、従業員(E)がエンドユーザー。

### 1.2 コアバリュー

| 価値 | 内容 | 主な受益者 |
|---|---|---|
| 価値A | 受検しやすさ(PC不要でスマホ受検) | 従業員 |
| 価値B | 通知配信(メール不要で通知到達、LINE経由)等による企業担当者の負担軽減 | 企業担当者 |

### 1.3 MVPの目的

両コアバリューを最小限の機能で検証する設計を志向する。

---

## 2. 技術スタック

| カテゴリ | 技術 |
|---|---|
| 言語・FW | Ruby 3.1.x / Rails 7.0.x |
| DB | PostgreSQL |
| 認証 | Devise / devise_invitable / 独自実装(受検者) |
| UI | Tailwind CSS |
| LINE連携 | line-bot-api gem / LINE Messaging API(Webhook受信・プッシュ通知送信) |
| QRコード | rqrcode gem |
| トークン生成 | SecureRandom |
| 開発環境 | Docker / ngrok(ローカル開発) |
| デプロイ | Render(予定) |
| 設計 | Figma(画面遷移図) |

---

## 3. ユーザーテーブル構成

| テーブル | 対象ユーザー | 認証方式 |
|---|---|---|
| `users` | システム管理者+企業担当者(roleカラムで区別) | Devise(メール+パスワード) |
| `employees` | 受検者(従業員) | 独自実装(受検者番号+生年月日→変更後パスワード) |

※本リリース版では`users`を`admin_users`/`company_users`に別テーブル化予定。

---

## 4. MVP全画面リスト

### 4.1 受検者向け画面(6画面)

| No | 画面名 | 備考 |
|---|---|---|
| E-1 | 初期登録画面 | 受検者番号+生年月日入力 |
| E-2 | 初期登録完了画面 | 表示のみ、ブラウザを閉じる |
| E-3 | ログイン画面 | 受検者番号+パスワード |
| E-4 | 強制パスワード変更画面 | 初回ログイン時のみ |
| E-5 | 受検画面 | 注意事項+57問4択+ログアウトボタン |
| E-6 | 結果表示画面 | ログアウトボタン配置 |

### 4.2 企業担当者向け画面(9画面)

| No | 画面名 | 備考 |
|---|---|---|
| B-1 | ログイン画面 | システム管理者と共通(S-1と同一) |
| B-2 | 操作メニュー一覧画面 | 5ボタン配置 |
| B-3 | 受検者一覧画面 | 個別CRUDボタン内包 |
| B-4 | 名簿アップロード画面(CSV) | 「追加」のみ |
| B-5 | 事前登録進捗確認画面 | 集計サマリ+全員リスト+ステータス |
| B-6 | 未受検者一覧画面 | 受検者番号、氏名、部署、事前登録状況 |
| B-7 | 招待受諾画面 | devise_invitable |
| B-8 | 受検者追加画面 | 個別CRUD用 |
| B-9 | 受検者編集画面 | 個別CRUD用 |

### 4.3 システム管理者向け画面(5画面)

| No | 画面名 | 備考 |
|---|---|---|
| S-1 | ログイン画面 | 企業担当者と共通(B-1と同一) |
| S-2 | 企業一覧画面 | ログイン後トップ |
| S-3 | 企業登録画面 | 企業情報+受検期間+初代担当者情報を入力 |
| S-4 | 企業詳細画面 | 企業情報+受検期間+初代担当者情報を表示+編集ボタン |
| S-5 | 企業編集画面 | 編集フォーム |

### 4.4 共通画面

| No | 画面名 | 備考 |
|---|---|---|
| C-1 | エラーページ(404) | |
| C-2 | エラーページ(500) | |

### 4.5 画面外要素(画面遷移図に描くが画面ではないもの)

- LINEメッセージ(初期登録案内、受検案内/受検勧奨)
- QRコード読み取りによるLINE友だち追加
- ログアウト処理(リダイレクトのみ)
- パスワードリセット(MVPでは運用対応=画面なし)

---

## 5. 受検者フロー詳細

### 5.1 フェーズ1: 事前登録期間(受検期間開始前)

```
[企業担当者配布の企業別QRコード]
        ↓ (スマホで読み取り)
[LINE友だち追加]
        ↓ (Webhookで企業ID+LINEユーザーIDをDB保存)
[LINEメッセージ受信: 初期登録案内]
   ※トークン付きURL
        ↓ (URLタップ)
[E-1 初期登録画面]
   ※受検者番号+生年月日入力
   ├─ 名簿照合失敗 → 同画面にエラー表示(リトライ制限なし)
   └─ 名簿照合成功 ↓
[E-2 初期登録完了画面]
   ※「初期登録完了。受検開始日をお待ちください。
       お疲れさまでした。ブラウザを閉じてください。」
        ↓ (ブラウザを閉じる)

   ===== 受検期間開始まで待機 =====
```

### 5.2 フェーズ2: 受検期間中

```
[企業担当者が「受検案内一斉配信」ボタン押下]
        ↓
[LINEメッセージ受信: 受検案内]
   ※ログイン画面へのURL
        ↓ (URLタップ)
[E-3 ログイン画面]
   ※受検者番号+パスワード入力
   ├─ 認証失敗 → 同画面にエラー表示
   └─ 認証成功 ↓
       状態で分岐
       │
       ├─ 初回ログイン → [E-4 強制パスワード変更画面]
       │       ※新パスワード+確認用パスワードの2つ
       │       ※要件:半角英数字混在8文字以上
       │       ↓ (変更完了)
       │
       ├─ パスワード変更済、未受検 → [E-5 受検画面]
       │       ※冒頭に注意事項箇条書き、57問4択
       │       ※ログアウトボタン(確認ポップアップ付き)
       │       ├─ 未回答あり → 同画面にエラー表示
       │       ├─ ログアウト → 確認ポップアップ
       │       │       ※「受検途中でログアウトすると回答は保存されず、
       │       │           最初からやり直しになります。よろしいでしょうか。」
       │       │       └─ はい → [E-3 ログイン画面]
       │       └─ 全問回答完了 → [E-6 結果表示画面]
       │              ↓ (ログアウト)
       │           [E-3 ログイン画面]
       │
       └─ 受検済 → [E-6 結果表示画面]に直接遷移
                       ↓ (ログアウト)
                    [E-3 ログイン画面]
```

### 5.3 受検者フローの主な仕様

| 項目 | 仕様 |
|---|---|
| 初期パスワード | 受検者の生年月日(YYYYMMDD) |
| パスワード要件 | 半角英数字混在8文字以上 |
| 初回ログイン時の挙動 | E-4強制パスワード変更画面に遷移 |
| 受検画面の注意事項 | 冒頭に箇条書き表示(無回答不可、ログアウトで回答消失等) |
| ログアウト時の挙動 | 受検中の回答は破棄(MVPでは途中保存なし) |
| 受検済者の再ログイン | E-6結果表示画面に直接遷移 |
| パスワード忘失対応 | MVPでは運用対応(企業担当者経由でシステム管理者に連絡→DB直接操作) |
| 受検画面のアクセス制限 | 受検期間中のみアクセス可能 |
| 結果表示画面の閲覧 | 受検期間後も閲覧可能(PDF保存等のため) |

---

## 6. 企業担当者フロー詳細

### 6.1 フェーズB1: アカウント開設期間

```
[システム管理者がS-3企業登録画面で送信]
        ↓ (devise_invitableが招待メール送信、有効期限7日間)
[企業担当者のメールボックスに招待メール到着]
   ※招待トークン付きURL
        ↓ (URLクリック)
   ┌─ 有効期限内 ↓
   │   [B-7 招待受諾画面(パスワード設定)]
   │   ※新パスワード+確認用パスワードを入力
   │   ※要件: 半角英数字混在8文字以上
   │       ↓ (パスワード設定完了)
   │   [B-2 操作メニュー一覧画面]に自動遷移
   │
   └─ 有効期限切れ → 期限切れエラー画面
       ※「招待の有効期限が切れています。
           システム管理者に再招待を依頼してください。」
```

### 6.2 フェーズB2: 受検期間開始前

#### B-2 操作メニュー一覧画面のボタン構成

| ボタン | 動作 |
|---|---|
| ①受検者一覧 | B-3画面に遷移 |
| ②事前登録進捗確認 | B-5画面に遷移 |
| ③未受検者一覧 | B-6画面に遷移 |
| ④QRコードダウンロード | 画面遷移なし、PNGファイルをダウンロード |
| ⑤受検案内一斉配信 | 画面遷移なし、確認ポップアップ→配信実行 |

#### 名簿アップロードフロー

```
[B-2 操作メニュー一覧画面]
        ↓ (①受検者一覧をクリック)
[B-3 受検者一覧画面 ※初回は空]
        ↓ (名簿アップロードボタンをクリック)
[B-4 名簿アップロード画面]
        ↓ (CSVファイル選択+アップロード)
   ├─ バリデーションエラー(1件でもあり) → エラー詳細を表示し全件拒否
   └─ 全件OK → 全件追加
[B-3 受検者一覧画面]
   ※フラッシュメッセージで「○○名を追加しました」
```

#### CSVカラム仕様(MVP版)

| No | カラム | 必須/任意 |
|---|---|---|
| ① | 受検者番号 | 必須 |
| ② | 氏名 | 必須 |
| ③ | 生年月日(YYYYMMDD) | 必須 |
| ④ | 性別 | 必須 |
| ⑤ | 部署 | 任意 |

※本リリース版で追加: 受検言語コード、受検方法、メールアドレス、集団分析カテゴリー1〜3
※CSVインポートはヘッダー行で対応付ける実装(列順依存をなくす)

#### 個別CRUDフロー

```
[B-3 受検者一覧画面]
├─ (追加ボタン) → [B-8 受検者追加画面] → 登録 → [B-3]
├─ (編集ボタン) → [B-9 受検者編集画面] → 更新 → [B-3]
└─ (削除ボタン) → 確認ポップアップ
                ※「○○さんを削除すると、過去の受検結果も
                    すべて削除されます。よろしいでしょうか。」
                ├─ キャンセル → [B-3]
                └─ 削除実行(物理削除) → [B-3]
```

#### CSVアップロードの重複処理

- アップロード仕様: **「追加」のみ**(全削除→上書きの新規アップロードは不採用)
- 重複時の挙動: **1件でも不具合あれば全件拒否**(案イ)
- 受検結果・LINEユーザーID・パスワードの消失リスクを設計レベルで解消

#### 個別CRUDの優先順位

編集 > 追加 > 削除 (実装の優先順位)

#### QRコードダウンロード

- MVP: PNG画像1枚(企業1社につき1つ)
- 本リリース版: PDFファイル

#### 事前登録進捗確認画面(B-5)

集計サマリ + 全員リスト + ステータス を1画面で表示

### 6.3 フェーズB3: 受検期間中

#### 受検案内/受検勧奨の一斉配信

```
[B-2 操作メニュー一覧画面]
        ↓ (運用ルール: 配信前に「受検者一覧」で対象者を目視確認)
        ↓ (⑤受検案内一斉配信ボタンをクリック)
[確認ポップアップ]
   ※配信対象人数を動的表示
   ※配信対象 = 事前登録完了 かつ 未受検
   ※「貴社の対象者○○名にLINEで受検案内を配信します。
       この操作は取り消せません。配信前に『受検者一覧』で
       対象者をご確認のうえ、よろしければ『配信する』を
       押してください。」
   ├─ キャンセル → [B-2]
   └─ 配信実行 → LINE Messaging API経由で一斉配信
           ↓
       [B-2 操作メニュー一覧画面]
       ※フラッシュメッセージで「○○名に配信しました」
```

#### 一斉配信の主な仕様

| 項目 | 仕様 |
|---|---|
| 配信対象 | 事前登録完了かつ未受検の受検者 |
| 押下回数制限 | なし(複数回配信可能=受検勧奨を兼ねる) |
| 配信メッセージ | MVPは1種類の固定文面(受検案内と受検勧奨を共通化) |
| 配信タイミング | 受検期間中のみ可能(期間外はボタン押下不可) |
| 本リリース版 | 案ウ(初回案内と勧奨で文面を分ける機能を実装) |

#### 未受検者一覧画面(B-6)

表示項目: 受検者番号、氏名、部署、事前登録状況

| 画面 | 表示対象 | 表示時期 |
|---|---|---|
| B-5 事前登録進捗確認画面 | 全受検者+事前登録の完了/未完了ステータス | 受検期間開始**前** |
| B-6 未受検者一覧画面 | 受検期間内の未受検者 | 受検期間**中** |

#### リマインダー機能

事前登録未完了者へのリマインダーは**MVPに含めず、運用でカバー**(企業担当者が社内で個別声掛け)。

---

## 7. システム管理者フロー詳細

### 7.1 アカウント作成

seedsファイルで初期データとして作成(Tさん自身が開発時に1名分作成)

### 7.2 フロー全体

```
[S-1 ログイン画面 ※企業担当者と共通]
        ↓ (システム管理者がログイン)
        ↓ (roleで分岐)
[S-2 企業一覧画面]
   ※表示項目: 企業ID(連番)+企業名
        ├─ (新規企業登録ボタン)
        │       ↓
        │   [S-3 企業登録画面]
        │   ※入力項目:
        │     ・企業名
        │     ・受検期間(開始日+終了日、日付のみ)
        │     ・初代担当者氏名
        │     ・初代担当者メールアドレス
        │       ↓ (登録ボタン)
        │   ├─ バリデーションエラー → 同画面にエラー表示
        │   └─ 登録成功 ↓
        │       (裏側でdevise_invitableが招待メール送信)
        │       ↓
        │   [S-2 企業一覧画面]に戻る
        │   ※フラッシュメッセージ「○○社を登録しました」
        │
        └─ (企業名をクリック)
                ↓
            [S-4 企業詳細画面]
            ※表示項目:
              ・企業ID
              ・企業名
              ・受検期間(開始日+終了日)
              ・初代担当者氏名
              ・初代担当者メールアドレス
              ・登録日時(任意)
            ※編集ボタン配置
            ※削除機能はMVPでは実装しない
                ↓ (編集ボタン)
            [S-5 企業編集画面]
                ↓ (更新ボタン)
            [S-4 企業詳細画面]に戻る
            ※フラッシュメッセージ「更新しました」
```

### 7.3 主な仕様

| 項目 | 仕様 |
|---|---|
| 企業ID | Railsデフォルト連番(5桁ゼロパディング表示なし) |
| 企業の削除機能 | MVPでは実装しない(編集のみ) |
| 受検期間のデータ構造 | MVPは1企業1実施回(companiesテーブルに直接保持) |
| 受検期間の粒度 | 日付のみ(時刻なし) |
| 本リリース版での発展 | 実施回テーブル(stress_check_periods)に分離、企業IDをUUID化 |

---

## 8. 受検期間と各機能の連動

| 機能 | 受検期間との連動 |
|---|---|
| E-5受検画面 | 受検期間中のみアクセス可能、期間外はエラー画面 |
| E-6結果表示画面 | 受検期間後も閲覧可能(PDF保存等のため) |
| 受検案内一斉配信ボタン | 受検期間中のみ配信可能 |

---

## 9. MVPと本リリース版の役割分担

### 9.1 MVPに含めるもの

- 受検者向けの基本機能(初期登録、ログイン、パスワード変更、受検、結果表示)
- 企業担当者向けの基本機能(名簿管理、QRコードDL、進捗確認、受検案内配信)
- システム管理者向けの基本機能(企業の登録・編集)
- LINE経由の一方向通知(初期登録案内、受検案内/受検勧奨)
- devise_invitable招待メール方式(企業担当者アカウント作成)

### 9.2 本リリース版に回すもの

- LIFF受検(LINE内で完結する受検フロー)
- 双方向LINEbot
- 医師面接申出勧奨通知
- 集団分析
- パスワードリセットUI(LINE Messaging API経由のリセットリンク送信)
- 中断保存・再開機能
- PDF出力機能
- 予約配信
- 回答確認画面(受検時の最終確認)
- 受検者一覧の検索・フィルタ機能
- 全受検者表示+ステータスフィルタ
- 集団分析カテゴリー(CSVカラム⑨〜⑪)
- 企業の削除機能(物理削除or論理削除)
- 企業IDのUUID化
- 実施回テーブルへの移行
- ユーザーテーブルの別テーブル方式への移行
- 配信メッセージのカスタマイズ(初回案内と受検勧奨で文面を分ける)
- 受検期間の時刻設定
- CSVテンプレートダウンロード
- 名簿アップロードのリトライ回数制限(10回程度)
- パスワード要件の強化(記号必須、10文字以上等)
- 集団分析機能

---

## 10. 設計上の重要な原則(議論を通じて確認された事項)

### 10.1 MVPスコープ規律

- 実装工数・運用現実性を根拠に機能を本リリース版に先送り
- その理由を明示的に記録する姿勢を一貫して保つ

### 10.2 コアバリューへの照合

- 設計判断は常にコアバリュー①(受検しやすさ)・②(通知配信等による企業担当者の負担軽減)への貢献度で検証

### 10.3 単一責任の原則

- 1つのカラム(役割)に複数の責任を持たせない設計を本リリース版で意識
- MVPではシンプルさを優先

### 10.4 段階的発展

- MVPでシンプルに実装→本リリース版で本格化、という段階的な発展を意識

---

## 11. 次のステップ

1. Figmaでの画面遷移図作成(本ドキュメントを設計図として参照)
2. GitHubでプルリクエスト作成(`feature/readme`ブランチからの派生想定)
3. PR画面にFigma URLを貼り付けて学校提出

---

**END OF DOCUMENT**
